# Binance Futures Testnet Trading Bot

Install:
```
pip install -r requirements.txt
```

Replace API_KEY and API_SECRET in bot/cli.py.

Example:
```
python -m bot.cli --symbol BTCUSDT --side BUY --type MARKET --quantity 0.001
```
